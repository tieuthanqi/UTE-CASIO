@model IEnumerable<DongHoCasio.Model.ChiTietDonHang>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutPage1.cshtml";
}

<table class="table">
    <tr>
        <th>
           Mã Đơn Hàng
        </th>
        <th>
           Mã Sản Phẩm
        </th>
        <th>
           Số Lượng
        </th>
        <th>
           Giá
        </th>
        
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.MaDH)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.MaSP)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.SoLuong)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Gia)
    </td>
    @*<td>
        @Html.ActionLink("Chỉnh Sửa", "Edit", new { id = item.ID } , htmlAttributes: new { @class = "btn-wide btn btn-success btn-sm" }) |
        @Html.ActionLink("Chi Tiết", "Details", new { id = item.ID }, htmlAttributes: new { @class = "btn badge-warning btn-sm" }) |
        @Html.ActionLink("Xóa", "Delete", new { id = item.ID }, htmlAttributes: new { @class = "btn badge-danger btn-sm")
    </td>*@
<td>
    @Html.ActionLink("Xóa", "Delete", new { id = item.ID }, htmlAttributes: new { @class = "btn badge-danger btn-sm" })
</td>
</tr>
}

</table>
